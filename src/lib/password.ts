/**
 * Largo mínimo de una contraseña. El mismo número tienen `create-employee`,
 * `update-employee` y la configuración de Auth del proyecto (Authentication → Sign In /
 * Providers → Email → Minimum password length). Si cambias uno, cambia los tres.
 */
export const MIN_PASSWORD_LENGTH = 8;
